## Packages
(none needed)

## Notes
Uses existing shadcn/ui + Tailwind setup already in repo
All API calls use @shared/routes (api + buildUrl) and validate with Zod safeParse + logging
Dark mode toggled via adding/removing `dark` class on document.documentElement (no extra deps)
